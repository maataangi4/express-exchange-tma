# Секреты (не коммитить)

При передаче проекта положите сюда или в стандартные пути:

| Файл | Куда скопировать |
|------|------------------|
| Service Account JSON | `../google-service-account.json` |
| `.env` с токенами | `../../.env.development.local` |

Шаблон переменных: `../../.env.example`  
Инструкция Google Sheets: `../GOOGLE_SHEETS_SETUP.md`

На этой машине ключ уже может быть в `server/google-service-account.json` (в git не попадает).
